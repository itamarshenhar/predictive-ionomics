# Pinned environment for HURCS cluster (Python 3.11).
# This mirrors slurm/setup_env.sh — keep the two in sync.
#
# IMPORTANT: install torch FIRST from the CUDA index, NOT from PyPI:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
# torch 2.9.1 is NOT on the cu124 wheel index; installing it from PyPI silently
# yields a CPU build -> config.DEVICE resolves to "cpu" -> TabPFN will not finish
# within the job walltime. cu124 runs on all HURCS GPUs (H200/A100/L40S/L4).
torch==2.6.0
torchvision==0.21.0
autogluon.tabular==1.5.0
tabpfn==8.0.3
shap==0.51.0
scikit-learn==1.7.2
numpy==2.1.3
pandas==2.3.3
lightgbm==4.6.0
xgboost==3.1.3
catboost==1.2.10
openpyxl==3.1.5
# Required to LOAD AutoGluon's FastAI ensemble members (fails late without it):
fastai
# Required by plotting.py:
seaborn
